# Checkpoint 1 reads process environment variables, not .env files automatically.
# Future service configuration will use this file with explicit loading.
AGENTIC_DEMO_DATA_DIR=./data
AGENTIC_DEMO_LOG_LEVEL=INFO
# Website credentials and model-provider configuration will be introduced
# with their implementations. Never put real secrets in this example.
